# KCEM 박물관 매출 웹 v1.2.0

## v1.1.0 변경사항

- 결제수단에 `시루` 추가
- 상세내역 결제수단: `현금 / 계좌 / 시루 / 카드`
- 일매출: 날짜칸을 넘기는 방식 대신 선택 월의 **건별 거래 리스트**
- 월매출: 실제 달력 형태
  - `현금` = 현금 + 계좌 + 시루
  - `카드` = 카드
  - 날짜별 총매출 표시
  - 매출이 있는 날짜 클릭 시 건별 상세로 이동
- 연매출: 1~12월 카드형 달력
  - 월별 현금 / 카드 / 총매출
  - 월 클릭 시 해당 월 달력으로 이동
- 기존 Excel의 2026-07-25 `(10000원)시루` 이관 데이터 보정
  - 기존 계좌 12,000원 -> 계좌 2,000원 + 시루 10,000원

---

## 1. Supabase DB 업그레이드

GitHub 파일을 올리기 전에 통합 Supabase SQL Editor에서 아래 파일을 먼저 실행합니다.

`supabase/KCEM_v1.1.0_SIRU_Upgrade.sql`

이 SQL은:

1. `kcem_sales.payment_method`에 `시루` 허용
2. 기존 시루 이관 내역 분리
3. 일/월/연 집계 View 재생성
4. PostgREST 스키마 캐시 갱신

을 처리합니다.

전체 매출 총액은 결제수단만 재분류하므로 변경되지 않습니다.

---

## 2. 웹 업데이트

기존 GitHub 저장소가 이미 있다면 이 폴더의 `GITHUB_DEPLOY.bat`을 실행합니다.

기본 저장소 이름은 `KCEM-Sales`이며, 기존 저장소가 있으면 새로 만들지 않고 변경 파일을 Push합니다.

GitHub Pages 배포가 완료되면 기존 주소가 그대로 업데이트됩니다.

---

## 3. 화면 집계 기준

### 일매출

선택한 월에 판매가 발생한 거래만 건별로 표시합니다.

- 날짜
- 시간
- 판매 품목
- 결제수단
- 금액
- 수량
- 비고

상단에는 결제수단을 실제 값 그대로 분리합니다.

`현금 / 계좌 / 시루 / 카드 / 총매출`

### 월매출

달력 각 날짜에는 아래 세 항목만 표시합니다.

- 현금 = 현금 + 계좌 + 시루
- 카드
- 총매출

매출이 없는 날짜는 `-`로 표시합니다.

### 연매출

12개월을 카드 형태로 표시합니다.

- 현금 = 현금 + 계좌 + 시루
- 카드
- 총매출

---

## 4. PIN 인증

기존 공용 PIN 인증을 그대로 사용합니다.

- 웹 소스에 PIN 저장 안 함
- PIN 성공 시 30일 조회 토큰 유지
- 웹은 조회 전용
- `kcem_sales`에 대한 anon 직접 SELECT 불가

---

## 5. 공통 DB 명명 규칙

- UWash: `uwash_*`
- OOZY Coffee: `oozy_*`
- KCEM Museum: `kcem_*`


---

## v1.2.0 UI 변경

- 한국공예체험박물관 로고를 헤더/PIN 화면에 적용
- 로고 색상 계열(적색/짙은 갈색)로 UI 리디자인
- 월/연도 드롭다운 제거
- 좌우 화살표만으로 월/연도 이동
- 기존 선택 UI 공간에 현금/카드/총매출 빠른 집계 배치
- 일매출은 해당 월 전체 건별 리스트 유지
- 건별 행 클릭 시 해당 날짜를 선택
- 일매출 상단 집계는 선택된 하루의 현금/계좌/시루/카드/총매출만 표시
- 월매출 달력 크기 확대 및 가독성 강화
- 월매출 집계: 현금 = 현금 + 계좌 + 시루 / 카드는 별도
- 연매출은 12개월 카드형 달력 유지 및 UI 개선
- 월 달력 날짜 클릭 → 해당 날짜를 선택한 일매출 화면으로 이동

DB 변경은 없습니다. 기존 `kcem_sales`, PIN RPC, Supabase 연결을 그대로 사용합니다.


---

## v1.2.1
- 상단 헤더 로고 이미지 제거
- 헤더 높이 축소
- `KCEM SALES · 박물관 매출관리` 텍스트만 유지
- 연결 상태 / 인증 해제 버튼도 소형화
- PIN 화면 로고 제거
- 매출 화면/DB/인증 로직 변경 없음


---

## v1.3.0

### 일매출
- 선택 날짜의 판매 품목 상세를 가장 먼저 표시
- 시간 / 품목 / 실제 결제수단 / 수량 / 금액 / 비고 표시
- 그 아래에는 선택 월 전체 건별 거래 목록 유지
- 전체 목록에서 행을 누르면 해당 날짜 상세로 전환

### 월매출
- 기존 달력 유지
- 달력 아래 `월간 품목별 집계` 추가
- 같은 `item_name`은 한 줄로 통합
- `quantity`를 합산하여 수량 증가
- 현금계 = 현금 + 계좌 + 시루
- 카드는 별도
- 총매출 및 거래건수 표시

### 연매출
- 12개월 카드 유지
- 아래에 `연간 품목별 집계` 추가
- 같은 품목은 연간 한 줄로 통합
- 수량 / 현금계 / 카드 / 총매출 / 거래건수 표시

### 인쇄
모든 탭에 `인쇄` 버튼 추가.

화면 자체를 캡처해서 인쇄하지 않고 A4용 Excel 표 형태의 전용 인쇄물을 생성합니다.

- 일매출 인쇄:
  - 선택일 매출 요약
  - 선택일 판매품목 상세
  - 해당 월 전체 건별 거래목록
- 월매출 인쇄:
  - 월 합계
  - 품목별 집계
  - 일자별 매출표
- 연매출 인쇄:
  - 연 합계
  - 1~12월 월별 매출표
  - 연간 품목별 집계

DB 구조 변경은 없습니다.
기존 `kcem_sales.quantity`를 기준으로 품목 수량을 누적합니다.


---

## v1.3.1 관리자 수정

웹 신규 등록은 계속 지원하지 않습니다.

### 일반 조회
- 기존 공용 PIN
- 일/월/연 매출 조회
- 인쇄

### 수정 / 삭제
건별 목록의 `수정` 버튼을 누르면 관리자 이메일 인증을 요구합니다.

Supabase Auth 계정으로 로그인 후 `kcem_user_roles.role = admin`인지 다시 확인합니다.

관리자만 수정 가능한 필드:
- 날짜
- 시간
- 판매 품목
- 결제수단: 현금 / 계좌 / 시루 / 카드
- 금액
- 수량
- 비고

관리자만 삭제 가능하며 삭제 전 확인창을 표시합니다.

관리자 인증은 현재 브라우저 세션 동안만 유지됩니다.
페이지를 새로 열면 다시 인증할 수 있습니다.

Supabase의 기존 RLS가 정상이라면 별도 DB 변경은 필요 없지만,
권한 확인이 필요하면 `supabase/KCEM_ADMIN_EDIT_VERIFY_v1.3.1.sql`을 실행하세요.


---

## v1.3.2 오늘 날짜 기본 표시

- 첫 접속 시 현재 월의 오늘 날짜를 무조건 선택
- 오늘 매출이 없어도 날짜를 표시
- 매출이 없으면 현금/계좌/시루/카드/총매출 모두 0원, 판매 품목 0건
- 월 전체 건별 리스트는 기존처럼 유지
- 다른 월에 매출이 전혀 없으면 해당 월 1일을 기본 선택


---

## v1.4.0 - 박물관 운영관리 / 업무공유 1차

사이트를 `박물관 매출관리`에서 `박물관 운영관리` 구조로 확장했습니다.

상위 메뉴:
- 매출
- 업무공유

### 업무공유 사용 전 1회
Supabase SQL Editor에서 다음 파일을 실행합니다.

`supabase/KCEM_TASK_SHARE_SETUP_v1.4.0.sql`

기존 공용 PIN을 그대로 사용하므로 별도 업무 로그인은 없습니다.
공용 PIN으로 접속한 팀원은 업무공유의 등록/수정/상태변경/삭제/팀원관리가 가능합니다.
매출 수정/삭제는 기존처럼 관리자 이메일 인증이 필요합니다.

### 첫 사용
1. 업무공유 탭 이동
2. 팀원관리
3. 팀원 4명 등록
4. 각 기기에서 `현재 작성자` 한 번 선택
5. 이후 해당 기기는 작성자 선택을 기억

### 업무공유 기능
- 전체 / 팀원별 탭
- 요청 / 진행중 / 완료 3단계 보드
- 분류 자유입력 (3D 프린트, 제작, 구매 등)
- 업무내용
- 선택 수량
- 담당자
- 상세메모
- 업무 수정 / 삭제
- 팀원 추가 / 이름수정 / 삭제(비활성) / 복원
- 5초 자동 갱신
- 누가 상태를 바꿨는지 DB history에 기록

업무 삭제는 실제 DB 행 삭제가 아니라 `deleted_at`을 사용하는 숨김 처리입니다.
팀원 삭제도 과거 업무 기록 보호를 위해 `is_active=false` 방식입니다.
