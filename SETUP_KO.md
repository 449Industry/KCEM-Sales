# KCEM 박물관 매출 웹 v1.2.0

## v1.1.0 변경사항

- 결제수단에 `시루` 추가
- 상세내역 결제수단: `현금 / 계좌 / 시루 / 카드`
- 일매출: 날짜칸을 넘기는 방식 대신 선택 월의 **건별 거래 리스트**
- 월매출: 실제 달력 형태
  - `현금` = 현금 + 계좌 + 시루
  - `카드` = 카드
  - 날짜별 총매출 표시
  - 매출이 있는 날짜 클릭 시 건별 상세로 이동
- 연매출: 1~12월 카드형 달력
  - 월별 현금 / 카드 / 총매출
  - 월 클릭 시 해당 월 달력으로 이동
- 기존 Excel의 2026-07-25 `(10000원)시루` 이관 데이터 보정
  - 기존 계좌 12,000원 -> 계좌 2,000원 + 시루 10,000원

---

## 1. Supabase DB 업그레이드

GitHub 파일을 올리기 전에 통합 Supabase SQL Editor에서 아래 파일을 먼저 실행합니다.

`supabase/KCEM_v1.1.0_SIRU_Upgrade.sql`

이 SQL은:

1. `kcem_sales.payment_method`에 `시루` 허용
2. 기존 시루 이관 내역 분리
3. 일/월/연 집계 View 재생성
4. PostgREST 스키마 캐시 갱신

을 처리합니다.

전체 매출 총액은 결제수단만 재분류하므로 변경되지 않습니다.

---

## 2. 웹 업데이트

기존 GitHub 저장소가 이미 있다면 이 폴더의 `GITHUB_DEPLOY.bat`을 실행합니다.

기본 저장소 이름은 `KCEM-Sales`이며, 기존 저장소가 있으면 새로 만들지 않고 변경 파일을 Push합니다.

GitHub Pages 배포가 완료되면 기존 주소가 그대로 업데이트됩니다.

---

## 3. 화면 집계 기준

### 일매출

선택한 월에 판매가 발생한 거래만 건별로 표시합니다.

- 날짜
- 시간
- 판매 품목
- 결제수단
- 금액
- 수량
- 비고

상단에는 결제수단을 실제 값 그대로 분리합니다.

`현금 / 계좌 / 시루 / 카드 / 총매출`

### 월매출

달력 각 날짜에는 아래 세 항목만 표시합니다.

- 현금 = 현금 + 계좌 + 시루
- 카드
- 총매출

매출이 없는 날짜는 `-`로 표시합니다.

### 연매출

12개월을 카드 형태로 표시합니다.

- 현금 = 현금 + 계좌 + 시루
- 카드
- 총매출

---

## 4. PIN 인증

기존 공용 PIN 인증을 그대로 사용합니다.

- 웹 소스에 PIN 저장 안 함
- PIN 성공 시 30일 조회 토큰 유지
- 웹은 조회 전용
- `kcem_sales`에 대한 anon 직접 SELECT 불가

---

## 5. 공통 DB 명명 규칙

- UWash: `uwash_*`
- OOZY Coffee: `oozy_*`
- KCEM Museum: `kcem_*`


---

## v1.2.0 UI 변경

- 한국공예체험박물관 로고를 헤더/PIN 화면에 적용
- 로고 색상 계열(적색/짙은 갈색)로 UI 리디자인
- 월/연도 드롭다운 제거
- 좌우 화살표만으로 월/연도 이동
- 기존 선택 UI 공간에 현금/카드/총매출 빠른 집계 배치
- 일매출은 해당 월 전체 건별 리스트 유지
- 건별 행 클릭 시 해당 날짜를 선택
- 일매출 상단 집계는 선택된 하루의 현금/계좌/시루/카드/총매출만 표시
- 월매출 달력 크기 확대 및 가독성 강화
- 월매출 집계: 현금 = 현금 + 계좌 + 시루 / 카드는 별도
- 연매출은 12개월 카드형 달력 유지 및 UI 개선
- 월 달력 날짜 클릭 → 해당 날짜를 선택한 일매출 화면으로 이동

DB 변경은 없습니다. 기존 `kcem_sales`, PIN RPC, Supabase 연결을 그대로 사용합니다.


---

## v1.2.1
- 상단 헤더 로고 이미지 제거
- 헤더 높이 축소
- `KCEM SALES · 박물관 매출관리` 텍스트만 유지
- 연결 상태 / 인증 해제 버튼도 소형화
- PIN 화면 로고 제거
- 매출 화면/DB/인증 로직 변경 없음


---

## v1.3.0

### 일매출
- 선택 날짜의 판매 품목 상세를 가장 먼저 표시
- 시간 / 품목 / 실제 결제수단 / 수량 / 금액 / 비고 표시
- 그 아래에는 선택 월 전체 건별 거래 목록 유지
- 전체 목록에서 행을 누르면 해당 날짜 상세로 전환

### 월매출
- 기존 달력 유지
- 달력 아래 `월간 품목별 집계` 추가
- 같은 `item_name`은 한 줄로 통합
- `quantity`를 합산하여 수량 증가
- 현금계 = 현금 + 계좌 + 시루
- 카드는 별도
- 총매출 및 거래건수 표시

### 연매출
- 12개월 카드 유지
- 아래에 `연간 품목별 집계` 추가
- 같은 품목은 연간 한 줄로 통합
- 수량 / 현금계 / 카드 / 총매출 / 거래건수 표시

### 인쇄
모든 탭에 `인쇄` 버튼 추가.

화면 자체를 캡처해서 인쇄하지 않고 A4용 Excel 표 형태의 전용 인쇄물을 생성합니다.

- 일매출 인쇄:
  - 선택일 매출 요약
  - 선택일 판매품목 상세
  - 해당 월 전체 건별 거래목록
- 월매출 인쇄:
  - 월 합계
  - 품목별 집계
  - 일자별 매출표
- 연매출 인쇄:
  - 연 합계
  - 1~12월 월별 매출표
  - 연간 품목별 집계

DB 구조 변경은 없습니다.
기존 `kcem_sales.quantity`를 기준으로 품목 수량을 누적합니다.
